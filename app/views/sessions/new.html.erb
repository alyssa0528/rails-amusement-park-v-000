<%= form_for :user do |f| %>
  <p>Your name: <%= f.collection_select :name, User.all, :name, :name, :prompt => 'Please select your username from this dropdown'%></p>
  <%= f.label :password, "Your password:" %>
  <%= f.password_field :password %>
  <%= f.submit "Sign In" %>
<% end %>
